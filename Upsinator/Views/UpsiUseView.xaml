<UserControl x:Class="Upsinator.Views.UpsiUseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Upsinator.Views"
      xmlns:viewModels="clr-namespace:Upsinator.ViewModels"
      xmlns:customControls="clr-namespace:Upsinator.CustomControls"
      xmlns:models="clr-namespace:Upsinator.Models"
      mc:Ignorable="d" >

    <UserControl.DataContext>
        <viewModels:UpsiUseViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Model.Upsi.Title}" FontWeight="Bold" Margin="5" />

        <ScrollViewer Grid.Row="1" Margin="5" >
            <ItemsControl ItemsSource="{Binding Model.Upsi.Args}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5, 0, 5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Label}" />

                            <!-- All the textboxes will be defined here
                                        and visibility based on the ArgType enum
                                        Opted to do this in my model at first, but principles decide it should be here
                                        as this handles the ViewStyling
                                        But i dont like the repetitiveness -->

                            <!-- ArgTypeEnum.String -->
                            <StackPanel Grid.Column="1">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter 
                                                Property="Visibility" 
                                                Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                    Binding="{Binding ArgType}"
                                                    Value="{x:Static models:ArgTypeEnum.String}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBox Text="{Binding Value}" />
                            </StackPanel>

                            <!-- ArgTypeEnum.File -->
                            <StackPanel Grid.Column="1">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter 
                                                Property="Visibility" 
                                                Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger 
                                                    Binding="{Binding ArgType}"
                                                    Value="{x:Static models:ArgTypeEnum.File}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <customControls:FilePathPicker Path="{Binding Value}" />
                            </StackPanel>

                            <!-- ArgTypeEnum.Folder -->
                            <StackPanel Grid.Column="1">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger  
                                                Binding="{Binding ArgType}"
                                                Value="{x:Static models:ArgTypeEnum.Folder}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <customControls:FolderPathPicker Path="{Binding Value}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Execute" Margin="5" Command="{Binding ExecuteCmd}" />
    </Grid>
</UserControl>
