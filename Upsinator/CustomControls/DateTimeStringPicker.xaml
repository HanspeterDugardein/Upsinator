<UserControl x:Class="Upsinator.CustomControls.DateTimeStringPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Upsinator.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance {x:Type local:DateTimeStringPicker}}">
    <Grid x:Name="RootUiElement">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DatePicker 
            SelectedDate="{Binding Date}"
            Grid.Column="0" 
            Height="22"
            />
        
        <TextBox 
            Text="{Binding Time}"
            Grid.Column="1" 
            Margin="5, 0, 0, 0"
            Height="22" />
    </Grid>
</UserControl>
